#include <iostream>
using namespace std;
int  sum(int a, int b){
    return a+b;             //if function is void so we can only calland if function is anything but not void so we can print the function
}
int main(){
    cout<<sum(3,5);

}